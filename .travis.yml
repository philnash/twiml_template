language: ruby
sudo: required
dist: trusty
before_install: gem install bundler
install:
  - bundle install --retry=3
cache: bundler
rvm:
  - 2.4.9
  - 2.5.7
  - 2.6.5
  - jruby-18mode
  - jruby-19mode
  - jruby-head
  - rbx-3
matrix:
  allow_failures:
addons:
  apt:
    packages:
      - haveged
